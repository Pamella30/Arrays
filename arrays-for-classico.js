const numeros = [100, 200, 300, 400]

for (let indice = 0; indice < numeros.length; indice++) {
    console.log(numeros[indice])
}

// Primeiro parametro foi criado uma variavel que e executada uma unica vez
// Segundo parametro e a condicao de execucao
// Terceiro parametro e executado no final fazendo o incremento